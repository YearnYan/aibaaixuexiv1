# 题目表格图形化任务状态

## 当前阶段

实现与验证完成。

## 已完成

- 定位根因：识别协议未定义表格图形类型，模型把表格转写为 Markdown。
- 确认现有题图裁切、实时预览、手动微调和 Word 图片导出链路可直接复用。
- 完成范围、数据契约、验证和回滚方案。
- 扩展图形契约为 `diagram | table`，并兼容独立 `tables` 字段和常见类型别名。
- 强化识别提示：所有原卷表格必须返回独立边界，不得转写为 Markdown 表格。
- 增加一次按需表格纠错复检；首次识别只有表格文字、没有边界时自动恢复表格题图。
- 实现 Markdown 表格安全去重：只有有效表格图形存在时才删除重复文本。
- 前端透传表格类型，复用题图裁切、实时预览、手动微调和 Word 导出链路。
- 增加多表、独立 `tables` 字段、纠错复检、正文保留和公式防误判回归。
- 使用用户截图完成浏览器页面验证。

## 进行中

- 无。

## 阻塞因素和未决风险

- 不同视觉模型对边界精度存在差异，现有人工裁切微调用于最终校正。
- 若首次识别和纠错复检都未返回有效边界，系统会保留表格文字并给出警告，不会静默丢失内容。

## 下一步

1. 后续发现新的模型表格字段别名时补入兼容映射和冒烟测试。
2. 将新增的复杂表格样本持续加入 `scripts/table-figure-regression.ts`。
