# 题卷重排识别与增量输出任务状态

## 纠偏修复（2026-07-30）

- [x] 移除主识别单页超时和结构耗尽后的原页占位成功结果。
- [x] 前端批次失败不再生成整页图片内容项，只保留失败页供再次识别。
- [x] 恢复 180 秒单请求、600 秒批次预算；多页超时立即拆分，单页仅重放一次。
- [x] 漏页和空页必须重试，最终失败沿用非 2xx 退款契约。
- [x] 题卷子站完整测试、类型检查、生产构建和平台退款测试通过。
- [x] 最终提交 `07252f4` 已推送并发布，87 个提交文件哈希一致，生产隔离会话创建返回 201。

## 卡顿修复任务（2026-07-28）

- [x] 增加上游请求和批次总时限，避免后台 Promise 无限等待。
- [x] 增加超时批次递归拆分与单页原图兜底。
- [x] 增加持续无响应上游的回归测试，并验证全页覆盖。

## 当前阶段

页面选择、5 页批次增量输出和按批次结算已实现；本轮主识别占位结果纠偏也已提交、推送并发布。

## 已完成

- 生产证据确认 116 页会话停在第 22 页附近，前三个批次断点有效。
- 确认坏批次多次获得 HTTP 200，但在批次内部结构或复检校验阶段失败。
- 定义阶段断点、单目标复检、自适应拆分和原页保真兜底契约。
- 主识别合法后立即写入 `partialBatches`，复检中断恢复时不再重跑主识别。
- 表格、图形选项和普通图形改为单目标、关联页复检，并在每个目标完成后更新断点。
- 所有结构类错误统一进入“同批一次纠错 → 递归缩小 → 单页原图保真”链路。
- 精确复检连续坏结构或传输失败时，保留目标关联页完整原图并继续生成。
- 新增 116 页故障注入测试，验证单坏页全覆盖、正常路径 17 次调用、复检不重跑主识别和阶段恢复。
- 题卷子站 TypeScript、全量测试和生产构建通过；统一平台 64/64 测试通过。
- 提交 `a840e1c` 已推送并部署，9 个发布文件逐项通过 SHA-256 校验。
- 生产远端类型检查与 116 页韧性故障注入通过。
- 仅切换题卷重排子进程；新进程 PID 66711 的父进程仍为未重启的平台 PID 61475。
- 5518 仅监听 `127.0.0.1`、在途连接归零，本机与公网健康检查正常。
- 回滚备份：`/opt/teacher-ai-empowerment/.deploy-backups/20260728-011356-a840e1c`。
- 生产证据确认会话 `word-a4672fd1-03b1-4b69-b221-bba7f2e05851` 已完成 116/116 页并生成 310 个内容项，故障发生在结果回传而非识别阶段。
- 完成请求改为 45 秒有界长轮询，处理中响应退款后自动重新接入同一后台 Promise。
- 增加结果交付互斥和 `finish/close` 状态转换，防止并发成功、重复扣分及退款后提前取走结果。
- 浏览器已覆盖处理中、已结算、网络断线与交付后状态回读。
- 目标 TypeScript、HTTP 前端恢复回归、后端有界长轮询冒烟测试通过。
- 题卷子站全量测试、Vite 生产构建和统一平台 64/64 全量测试通过。
- 提交 `aaddfd2` 已推送并部署，10 个发布文件逐项通过 SHA-256 校验。
- 生产远端 TypeScript、目标回归、后端冒烟和 Vite 构建通过；真实生产环境变量的隔离启动健康正常。
- 发布备份：`/opt/teacher-ai-empowerment/.deploy-backups/20260728-020243-aaddfd2`。
- 旧题卷子进程已安全退出，平台 PID 保持 `61475`；5518 按平台既有架构在首次访问时加载新代码，其他子站未重载。
- 生产 `word-layout` 未结算使用记录为 0。
- 上传后的 PDF/图片页面改为中央全屏预览，页面默认全选、带序号和勾选态，并支持一键全选。
- 识别界面隐藏“上传图片”阶段，统一显示“正在识别第 X/Y 页”。
- 选中页面按最多 5 页拆批；每批成功后即时追加 A4 结果，识别中允许导出当前结果。
- 混合 DOCX 首批携带 Word 页数完成核验会话，后续图片批次不重复计入 Word 页数。
- 同一识别会话的处理中重连复用稳定幂等键，平台只保留一笔预扣积分；后台失败后才换键重试，避免退款后无法再次识别。
- 子站对 `session_processing`、结果交付竞争明确返回延后结算标记；默认完成等待窗口提升至 170 秒，覆盖真实 AI 操作预算。
- 题卷子站类型检查、生产构建、现有全量回归和桌面/移动页面回归通过。
- 已提交并推送 `9c734eb` 到 `origin/main`。
- 已完成生产发布 `20260728-135604-9c734eb`，回滚备份位于 `/opt/teacher-ai-empowerment/.deploy-backups/20260728-135604-9c734eb/word`。
- 生产 5518 子站已切换到新进程，统一平台 PID `61475` 未重启；远端类型检查、题卷健康、平台健康和公网入口检查通过。
- 主识别占位结果纠偏最终发布为 `20260730-171515-07252f4`，回滚备份位于 `/opt/teacher-ai-empowerment/.deploy-backups/20260730-171515-07252f4`。
- 本次仅停止旧 5518 子进程并保持按需加载，统一平台 PID `135985` 未重启；生产完整测试、构建、87 文件哈希、本机/公网健康和隔离会话创建均通过。

## 进行中

- 无。

## 阻塞因素和未决风险

- 浏览器 CLI 通过 npx 安装时超时，已改用缓存中的 Playwright Chromium 完成只读页面检查；不影响构建和业务回归。
- 若混合 DOCX 首批在上游完全不可用，Word 会话会等待过期；图片批次仍按失败退款，生产环境继续观察该极端链路。
- 原页保真只允许用于已有文字内容的图形增强复检；主识别失败始终按退款契约处理。

## 下一步

1. 观察新任务的 `session_processing` 自动重连和最终结果交付状态。
2. 使用截图对应的 5 页试卷完成一次真实供应商识别，确认不再产生主识别原页占位结果。
3. 若用户网络在结果返回瞬间中断，使用同一页面等待状态接口自动回读，不需重新上传或重复识别。
