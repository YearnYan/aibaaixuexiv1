# 图形选项组任务状态

## 当前阶段

已完成并通过自动化、浏览器和 Word 结构验收。

## 已完成

- 定位根因：视觉选项被同时输出为空 Markdown 图片和普通题图。
- 确认完整选项组图片比拆分空链接更能保留 A/B/C/D 与图案对应关系。
- 完成范围、数据契约、验证和回滚设计。
- 扩展图形类型为 `diagram | table | option-group`，并兼容常见类型值和中文说明推断。
- 强化识别提示：纯视觉选项必须返回包含标签的完整选项组，`options` 不得输出空图片链接。
- 实现视觉占位选项检测，只在有效选项组存在时过滤空 Markdown 图片、失败提示和标签空壳。
- 增加一次按需图形选项边界复检，复检失败时保留原选项并返回警告。
- 空 Codecogs 公式图片不再请求并渲染 `Blank Equation`，改为可理解的本地降级文字。
- 优化图形选项组显示宽度、类型标签、裁切标题和实时预览。
- 增加后端、共享文本、Word 图片导出和浏览器页面回归。
- 用户复核确认：仅保留一张合并选项图仍然不符合选择题结构，必须显示四个选项。
- 增加单个 `option` 图形和 `optionLabel` 契约。
- 网页实现四个“标签 + 图片”选项卡及单项裁切。
- Word 实现四个视觉选项单元格。
- 将合并图改为保留 A/B/C/D 标签的回退模式。
- 浏览器验证桌面两列、窄屏单列、四个标签、四张图片、单项裁切和动态预览。
- 实际网页下载 Word 验证 A/B/C/D、四个绘图节点和四个媒体资源完整存在。
- `npm run typecheck`、`npm test`、`npm run build`、`git diff --check` 全部通过。

## 阻塞因素和未决风险

- 视觉模型边界可能包含题干或遗漏标签，现有人工裁切微调用于最终校正。
- 混合文字与图片选项采用保守过滤，仅删除可证明为占位的内容。

## 后续观察

- 继续收集真实试卷中 1×4、2×2、非规则排列视觉选项的边界质量；识别偏差可直接使用现有单项裁切微调校正。
