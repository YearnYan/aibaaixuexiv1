# 独立站验证记录

验证时间：2026-07-14（Asia/Shanghai）

## 自动检查

- `npm run typecheck`：通过。
- `npm test`：通过。覆盖管理员登录、加密配置保存、完整视觉连接验证、分批页码回填、归一化题图坐标、跨主机密钥保护和损坏配置恢复。
- 识别结果兼容回归：通过。覆盖 `response_format` 和输出上限发送、接口明确不支持时的一次降级重试、代码块与二次序列化 JSON、字段别名和缺失字段补齐、单页页码补齐、像素坐标归一化及非法题图局部忽略。
- 表格题图回归：通过。覆盖表格类型、独立 `tables` 字段、Markdown 表格按需纠错复检、有效题图存在后的文字去重、无边界时保留原文，以及同题多表按顺序裁切。
- 图形选项回归：通过。覆盖旧说明类型推断、空图片选项恢复为 A/B/C/D、缺失独立选项图按需复检、复检失败保留标签与合并图、混合文字/公式选项防误删，以及客户端和服务端 Word 四个独立视觉选项导出。
- `npm run build`：通过。生成 `dist/index.html`、`dist/admin.html` 与 `.server-dist/server/index.js`。
- `npm start`：通过。生产启动脚本可直接托管工作台 `/` 与管理员路由 `/admin/ai-settings`，两者均返回 `200`。
- 前台安全扫描：通过。扫描 11 个普通用户可访问源文件，未发现接口配置、密钥或直连信息泄露。

## 浏览器闭环

使用本地模拟识别服务和已编译服务端验证，不使用真实密钥：

1. 访问 `/admin/ai-settings`，未登录状态显示管理员登录页。
2. 登录后保存接口地址、密钥与模型；页面只显示“当前密钥已安全保存”，没有回显完整密钥。
3. 执行连接验证，状态更新为“连接已验证”并显示最近验证时间。
4. 访问普通工作台，上传测试 PNG，点击“智能识别”，得到可编辑题号、题干与选项。
5. 检查浏览器 `localStorage`，结果为空。
6. 在题目中的图形点击“裁切微调”，裁切框原位显示在 A4 题目预览中；右侧仅保留图形预览、重新裁切和移除操作。
7. 验证拖动边界、像素级方向微调、取消和应用裁切：取消后恢复题图入口，应用后更新题图并自动收起编辑器。
8. 模拟返回四个 `kind=option` 视觉选项，页面显示 A/B/C/D 四张“标签 + 图片”卡片；桌面为两列，窄屏为单列，均无横向滚动。
9. 点击 C 选项只打开“C 选项裁切微调”，页面只有一个裁切编辑器，其他三个选项仍可见；右移 1 像素后实时预览样式立即变化。
10. 从网页实际点击“导出 Word”，解压下载文件确认 A/B/C/D 四个标签、4 个 `<w:drawing>` 和 4 个 `word/media` 图片资源均存在，且无 `Blank Equation`、Codecogs 或 Markdown 图片链接。

## 视口检查

- 桌面 `1440x900`：工作台与配置页均完成截图检查，主任务、配置状态和编辑区可见，无重叠。
- 移动 `390x844`：工作台和配置页按单列流程显示，文本可读、操作可点击。
- 移动 `390x844` 原位裁切：裁切框、四边与四角手柄、微调工具及“应用裁切 / 取消”操作均完整可见，按钮文本不换行。
- 视觉选项 `1180x900`：四个选项按两列展示，文档宽度与视口宽度一致，无横向溢出。
- 视觉选项 `720x900`：四个选项自动切换为单列，四张卡片横坐标一致，无横向溢出。

截图归档于 `output/playwright/`：

- `workbench-desktop.png`
- `workbench-mobile.png`
- `admin-desktop.png`
- `admin-mobile.png`

## 已知边界

- 识别服务要求兼容 Chat Completions 请求格式且支持图片输入与 JSON 结构化输出。
- 题目识别结果是可编辑草稿，题目内容、题图边界、顺序和最终 Word 导出仍由教务人员复核。
