# 公式跨端互操作任务状态

## 当前阶段

实现与自动化验证完成。

## 已完成

- 确认客户端下载实际使用 `src/lib/wordExport.ts`。
- 确认服务端存在等价导出路径，需要保持规则一致。
- 复现并定位现有导出器的全有或全无异常策略。
- 建立本功能的设计与契约文档。
- 新增共享可移植 LaTeX 规范、控制字符清洗和可读公式降级。
- 网页端对每条公式进行 KaTeX 预检，失败时不再显示裸 LaTeX。
- 客户端和服务端导出器改为逐公式 OMML 优先、可读 OMML 兜底，并缓存重复公式转换。
- 修复方程组、矩阵和多行显示公式被普通换行切断的问题。
- 新增跨学科 25 条语料和 810 题混合导出回归，覆盖客户端与服务端 DOCX。
- 将 Codecogs、math.now.sh、Google Chart 等可信公式图片 Markdown 解码为可移植公式，不再把图片链接原样写入 Word。
- 将 `\\boldsymbol`、`\\bm` 等跨渲染器样式别名统一为可转换命令，并保留 Word 粗体向量语义。
- 清理 Word 正文中的 Markdown 图片、链接和表格分隔符，避免表示层语法裸露。
- 用 XML 解析验证回归 DOCX 可打开、无占位符、无原始 LaTeX、无替换字符且保留 OMML 和粗体数学结构。

## 进行中

- 无。

## 风险与待确认项

- 第三方 LaTeX 宏包和用户自定义命令无法保证结构化 OMML，但必须保证可读降级。
- 本机 Word COM 能打开回归 DOCX并识别为 5 页，但后台 PDF 导出在当前桌面运行时未返回文件；已完成 XML 结构与 Word 打开验证，视觉渲染需在可用 Office 导出环境复核。

## 下一步

1. 在正常 Office 导出环境抽查回归文档页面截图。
2. 后续新增学科公式时，将案例补入 `scripts/formula-regression.ts`。
