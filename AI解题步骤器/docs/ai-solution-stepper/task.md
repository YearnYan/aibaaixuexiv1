# 当前任务状态

## 当前阶段

内容规范版本 2、完整答案模块、PDF 原子分页、可编辑 Word 和科学内容韧性修复已实现并完成验收，最新服务运行在 8789 端口。

## 已完成

- 根据用户修正，确定“上传后 AI 一次性给出四步完整指导和完整答案”的最终范围。
- 检查参考 UI 的 1536×1024 基准尺寸与桌面布局。
- 确定配置加密和四小时只读结果会话边界。
- 定义四步 `guidance` 与三级公开指导的前后端契约。
- 完成 React 工作台、独立 AI 配置页和桌面/移动响应式样式。
- 完成 PDF、DOCX、图片和文本解析，以及 OpenAI 兼容调用。
- 移除步骤作答、AI 判分、提示申请、答案解锁和同类题验证链路。
- 完成四步 AI 指导直出、步骤切换和刷新恢复。
- 11 个单元与 HTTP 测试通过，ESLint、生产构建和依赖审计通过。
- Playwright 已验证 1536×1024 与 390×844 视口无横向溢出。
- 已确定科学内容必须在进入会话前严格校验，网页和两种下载格式共用 KaTeX 渲染结果。
- 内容响应已升级到版本 2，旧会话会被清除，裸拉丁变量、单位、点名和缩写无法进入会话。
- 页面已增加第 5 个“完整答案”模块，删除旧答案锁区域。
- PDF 已按逻辑块整体换页，3 页实测报告无内容切割。
- DOCX 已改为原生可编辑正文和 OMML 公式，媒体图片为 0；跨学科样本包含分数、根式、上下标和化学式结构。
- `npm run check` 在 17.3 秒内通过，32 个测试全部通过。
- Playwright 已验证桌面、390px 手机、旧会话拦截和两种真实下载。
- 已使用真实 `地理题1.png` 复现中文中误插 `the` 导致的错误，并实现噪声预清理、违规片段反馈、最多三轮增量修复和无进展升级。
- 真实地理题已重新调用当前 AI 链路并成功返回规范 LaTeX 结果；真实学科缩写仍受严格校验保护。
- `npm run check` 已通过，38 个测试全部通过；Playwright 真实上传同一张地理题后成功生成，无旧公式错误或英文噪声残留。

## 进行中

- 无。

## 阻塞与风险

- 当前服务已配置 AI 模型；真实地理题已完成服务端和浏览器完整链路验收。
- 不同 OpenAI 兼容服务对图片输入支持不同，上传图片时模型需要具备视觉能力。
- 不同 WPS/Word 版本对极少数复杂 MathML 结构的视觉细节可能略有差异；当前实现保持内容可编辑，并禁止退回整页图片。

## 下一步

1. 使用真实业务题目在 8789 端口重新生成，旧会话不会继续显示。
2. 根据实际模型输出继续扩充对抗样例，但不得放宽科学符号校验。
