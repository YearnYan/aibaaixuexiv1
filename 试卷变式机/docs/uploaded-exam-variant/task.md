# 当前任务

## 当前阶段

题图完整性恢复已完成。

## 已完成

- 已梳理原有配置式出卷和预览导出链路。
- 已确定上传文件契约和大小边界。
- 已添加本地 PDF、DOCX、DOC 抽取依赖。
- 已实现本地 Node 上传解析和变式出卷接口。
- 已将左侧设置栏改造为大上传框。
- 已保留 A4 预览、答案显示和导出能力。
- 已完成前端构建、Worker 构建和 Playwright 页面快检。
- 已在左侧上传区下方新增变式难度和变式系数设置项。
- 已将变式难度和变式系数接入本地服务与 Worker 的上传变式出卷提示词。
- 已完成前端构建和 Worker 构建验证。
- 已强化变式系数规则：1-5 级偏保守，6-10 级只锁定知识点和考点，允许从题型、情境、条件、设问、表达载体和推理路径等维度做强变式。
- 已强化变式难度规则：补充 1-10 级逐级区分，并要求模型按步骤数、综合性、隐藏条件和创新度自检。
- 已提升高变式系数变化下限：6-7 级至少改变 4 个维度，8-9 级至少改变 5-6 个维度，10 级要求接近重构式变体。
- 已确认当前失败由“题干引用视觉材料但缺少可还原 figure.description”触发，不是科目或上传接口失败。
- 已确定共享题图契约、定向补图和最终自包含改写三层恢复策略。
- 已将共享契约接入 Node 与 Worker 的普通出卷、上传原卷变式出卷链路。
- 已补齐 Worker 上传输出中的 `figure` 结构，并将生成令牌上限与 Node 链路统一为 16000。
- 已实现两轮逐题恢复：首轮补全精准题图，第二轮强制改写为自包含题。
- 已在修复后重建题号和答案，最终执行题图完整性断言。
- 已完成 11 项测试、Node 语法检查、Worker 构建和完整项目构建。

## 风险

- Worker 环境仅支持图片上传解析；PDF 和 Word 需要本地 Node 服务。
- 本机未配置真实 AI Key，未执行真实 AI 生成端到端验证。
- 真实难度与变式幅度仍依赖模型遵循提示词，需要用样卷人工验收。
- 当前项目未安装 Playwright 包，本次未完成浏览器自动化快检。
- 高变式系数允许题型等更多维度变化，验收时需确认知识点和考点保持一致，同时变化幅度足够明显。
- 高变式效果仍需用真实原卷抽样验证，重点检查是否只换数字或同义改写。
- 真实管理员模型仍可能在两轮定向修复中持续违反结构契约；此时必须明确失败，不能返回无解题。

## 后续验收

- 部署或重载试卷变式机子进程后，使用包含地图、实验装置、统计图、漫画和几何图的真实原卷抽样验收。
- 通过返回元数据中的 `figureIntegrityIssueCount` 和 `figureRepairedIndexes` 定位发生过题图修复的题目。
