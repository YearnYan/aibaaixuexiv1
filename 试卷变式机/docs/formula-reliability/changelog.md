# 公式生成可靠性变更记录

- 日期：2026-07-29
- 摘要：建立公式字段三级可靠性闭环。
- 接触点：`shared/academic-content.js`、Node 生成服务、Worker 生成路由、学术内容测试
- 行为：自动识别并修复混用、漏闭合、孤立闭合、错序、嵌套和非标准美元定界符；只返修坏字段，返修失败时安全移除孤立定界符，不再让局部格式错误导致整卷失败。

## 生产部署

- 代码提交：`8751922 fix: 建立试卷变式机公式可靠性闭环`
- 服务器全量测试：35/35 通过。
- Worker 构建：成功生成 `dist/_worker.js`。
- 服务切换：`teacher-ai-platform` 已重启并加载新代码，主进程 PID 从
  `118298` 更新为 `119295`。
- 健康验证：服务器本机与 `https://jiaoshiai.top/api/health` 均返回
  `ok: true`，重启后三分钟内无错误级日志。
- 真实入口验证：通过已有登录态刷新
  `https://jiaoshiai.top/apps/variant-exam/`，页面完整加载且浏览器控制台无错误。
- 子站运行验证：平台按授权访问懒启动试卷变式机，子进程 PID 为 `119599`，
  由新平台进程 `119295` 管理；`127.0.0.1:5534/api/health` 返回
  `status: ok`，最后一次启动后无新增非空日志。
- 范围确认：实现、测试、文档和部署文件均仅位于“试卷变式机”子网站。
