# 全学科公式渲染任务状态

## 当前阶段

实现与验收已完成。

## 已完成

- 定位后端破坏性 LaTeX 转 Unicode 后处理。
- 定位 JSON 合法转义误吞 LaTeX 命令的风险。
- 定位构建模板未包含 KaTeX，现有根页面依赖 CDN 且与构建模板不一致。
- 完成范围、内容契约和验证策略设计。
- 新增共享学术内容 JSON 安全解析、规范化、答案重建与完整性校验模块。
- Node 与 Worker 已统一使用全学科公式契约，并删除 Unicode 降级后处理。
- 前端已本地捆绑 KaTeX 0.16.22 与 mhchem，网页使用 HTML+MathML，Word 使用 MathML。
- 新增五大学科公式回归测试；现有测试 22 项全部通过。
- 前端与 Worker 构建成功，真实浏览器验收无公式渲染错误。

## 进行中

- 无。

## 阻塞因素和未决风险

- 无阻塞。
- Word 继续采用旧式 HTML `.doc` 容器；公式主体改为标准 MathML，并保留异常公式的可读降级。

## 下一步

1. 上线后抽查真实 AI 返回的不同学科试卷。
2. 若后续改为 `.docx` 导出，应将 MathML 转换为原生 OMML 公式对象。
