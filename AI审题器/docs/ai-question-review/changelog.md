# 实施记录

## 2026-07-17

- 摘要：建立 AI 审题网站的工程骨架与契约文档。
- 接触点：项目配置、`docs/ai-question-review/`
- 行为：定义前后端构建流程、AI 配置边界、审题接口和结构化结果模型。

## 2026-07-17（实现）

- 摘要：完成可运行的审题工作台、AI 服务端与独立配置页面。
- 接触点：`src/`、`server/`、`tests/`、`README.md`
- 行为：支持多文件上传、PDF/DOCX/图片解析、真实兼容模型调用、结果校验、配置鉴权、连接测试、桌面与移动响应式展示。

## 2026-07-17（验收）

- 摘要：完成自动化检查和多视口视觉验收。
- 接触点：构建配置、测试、桌面与移动工作台、配置页
- 行为：7 项单元测试通过，类型检查与生产构建通过；1536×1024 和 390×844 视口无横向溢出，上传、未配置错误提示和配置表单交互正常。

## 2026-07-17（公式与导出设计）

- 摘要：新增跨学科 LaTeX 渲染和 PDF/Word 报告导出契约。
- 接触点：AI 输出规范、报告数据模型、导出 API、用户侧结果工具栏
- 行为：统一公式分隔符与化学式语法，定义无状态双格式导出、Chromium 运行依赖和文档验收标准；用户侧隐藏 AI 配置入口。

## 2026-07-17（公式与导出实现）

- 摘要：完成跨学科公式全量渲染、双格式报告导出和下载交互。
- 接触点：`shared/latex.ts`、`src/components/RichText.tsx`、`src/components/ResultPanel.tsx`、`server/report-*.ts`、`server/ai.ts`、Docker 与测试。
- 行为：网页使用 KaTeX/mhchem；AI 结果执行全字段公式校验与一次自动修复；PDF 使用 Chromium 打印完整报告；Word 使用透明公式图片；用户页面隐藏 AI 配置入口。

## 2026-07-17（公式与导出验收）

- 摘要：完成真实浏览器、真实下载和跨学科报告验收。
- 接触点：1440×1000 桌面视口、390×844 手机视口、PDF/DOCX 下载文件、`tests/latex.test.ts`、`tests/report-export.test.ts`。
- 行为：12 项测试、类型检查和生产构建通过；数学、物理、化学、生物、地理公式均正常；长化学式溢出已修复；PDF 逐页无缺字、重叠或截断；下载所得 PDF/DOCX 文件头、大小、公式图片与引用均通过校验。

## 2026-07-17（可编辑公式与结果区精修设计）

- 摘要：确定 Word 原生 OMML、文件集合替换、高亮框包含和遗漏提示下移方案。
- 接触点：`server/report-word.ts`、`server/word-math.ts`、`src/components/InputPanel.tsx`、`src/components/ResultPanel.tsx`、`src/styles.css`。
- 行为：Word 禁止图片公式；重新上传不累计旧文件；遗漏提示调整到题目预览下方；公式高亮边框按真实内容高度排版。

## 2026-07-17（可编辑公式与结果区精修实现）

- 摘要：完成 Word 图片公式到原生 OMML 的迁移，并收口上传与结果区交互。
- 接触点：`server/word-math.ts`、`server/report-word.ts`、`src/components/InputPanel.tsx`、`src/components/ResultPanel.tsx`、`src/styles.css`、导出测试。
- 行为：LaTeX 经 MathML 与结构化 AST 转为可编辑 Office 公式；新文件批次整体替换旧批次；遗漏提示位于题目预览后；含公式高亮框按可见内容高度与宽度排版。

## 2026-07-17（可编辑公式与结果区精修验收）

- 摘要：完成静态、自动化、真实浏览器与真实下载四层对抗验收。
- 接触点：1536×1024 桌面视口、390×844 手机视口、真实下载 DOCX、19 项自动化测试和生产构建。
- 行为：两端均无横向溢出，7 个含公式高亮框完整包含可见公式，连续上传后仅保留新文件；真实 DOCX 含 23 个 `m:oMath`，且无 `<undefined>`、`w:drawing` 或 `word/media`。
